# 疫见全球 · 依赖清单(VPS: pip3 install -r requirements.txt)
openpyxl>=3.0      # Excel 导出(可选, 缺失时 report.py 自动降级 CSV)
python-docx>=1.1   # Word 情报简报(缺失时 report.py 自动跳过)
flask>=3.0         # 疫情雷达面板 webapp/app.py
